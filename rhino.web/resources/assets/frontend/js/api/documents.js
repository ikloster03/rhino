import axios from 'axios'

// const PATH = `/api/documents`

export const getNews = async (page = 1, perPage = 4) => {
    // return await axios.get(`${PATH}/posts?page=${page}&per_page=${perPage}`)
    return {
        data: [
            
        ]
    }
}

